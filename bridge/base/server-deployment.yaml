#! server-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: server
    namespace: midwifery
    labels:
        com.docker.compose.project: midwifery
        com.docker.compose.service: server
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: midwifery
            com.docker.compose.service: server
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: midwifery
                com.docker.compose.service: server
                com.docker.compose.network.default: "true"
        spec:
            containers:
                - name: server
                  image: midwifery-server
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: ADMIN_EMAIL
                      value: "admin@midwifery.edu"
                    - name: ADMIN_PASSWORD
                      value: "midwifery123!"
                    - name: CLIENT_URL
                      value: "http://localhost:3000"
                    - name: MONGODB_URI
                      value: "mongodb://admin:password@mongo:27017/admin"
                    - name: SESSION_SECRET
                      value: "your-session-secret"
                  ports:
                    - name: server-4000
                      containerPort: 4000
